$(function () {
    $(".shop_orderDetail_status span.lf:contains('待发货')").html("订单状态：海关清关中");

    // var orderStatusText = $(".shop_orderDetail_status span.lf").text();
    // if (orderStatusText.indexOf("待发货") >= 0) {
    //     $(".shop_orderDetail_status span.lf").html("订单状态：海关清关中");
    // }

});
